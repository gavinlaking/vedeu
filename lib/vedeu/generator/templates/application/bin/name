#!/usr/bin/env ruby

lib_dir = File.dirname(__FILE__) + '/../lib'
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)

require '<%= object.name %>'

args = ARGV.dup

<%= object.name_as_class %>::Application.start(args)
