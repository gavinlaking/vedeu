module <%= object.app_name %>

  class <%= object.name_as_class %>View < Vedeu::ApplicationView

    def render
      Vedeu.renders do
        template_for('<%= object.name %>', template, object, options)
      end
    end

    private

    def options
      {}
    end

  end

end
