### `enable!`

Enable the border:
(Borders are enabled by default when defined for an interface).

    Vedeu.border :border_demo do
      enable!
      # ... some code
    end

or

    Vedeu.border :border_demo do
      enabled!
      # ... some code
    end
